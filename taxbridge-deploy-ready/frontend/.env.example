# Copy this to .env for local development, or set REACT_APP_BACKEND_URL
# as an Environment Variable in your Vercel project settings for production.
# No trailing slash, no /api suffix - just the backend's base URL.
REACT_APP_BACKEND_URL=http://localhost:8000
WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false
